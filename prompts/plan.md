---
description: Turn a feature PRD into independently-grabbable local Markdown plan files using tracer-bullet slices
argument-hint: "[prd-path-or-topic]"
---
Load the `to-plan` skill and run it. Args: $@
